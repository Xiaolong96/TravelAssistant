<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <item name="colorPrimaryDark">@color/main_color</item>
        <!--<item name="colorAccent">@color/colorAccent</item>-->
        <item name="android:statusBarColor">@color/main_color</item>
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--去掉系统自带标题栏-->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!--设置半透明虚拟键盘-->
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="fullScreen" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>
    <style name="IntroTheme" parent="AppTheme">
        <!--设置背景图片-->
        <!--<item name="android:background">@drawable/intro_background</item>-->
        <!--设置屏幕全屏,最顶部的状态栏会消失-->
        <!--<item name="android:windowFullscreen">true</item>-->
        <!--设置半透明状态栏-->
        <!--<item name="android:windowTranslucentStatus">true</item>-->
        <!--设置半透明虚拟键盘-->
        <!--<item name="android:windowTranslucentNavigation">true</item>-->
        <!--Android 5.x开始需要把颜色设置透明，否则导航栏会呈现系统默认的浅灰色-->
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <!--防止布局和状态栏的信息重叠需在xml布局文件中加上 android:fitsSystemWindows=”true”-->
    </style>

    <!--全局通用样式-->
    <style name="line">
        <item name="android:layout_height">1dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginLeft">@dimen/margin_size</item>
        <item name="android:layout_marginRight">@dimen/margin_size</item>
        <item name="android:background">@color/line</item>
    </style>

</resources>
